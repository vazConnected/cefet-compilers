class Teste2 
/* Teste de comentário 
com mais de uma linha */ 
  int a, valor, b_1, b_2; 
  { 
     write("Entre com o valor de a: "); 
     read (a); 
     b_1 = a * a; 
     write("O valor de b1 e: "); 
     write (b_1); 
     b_2 = b_1 + a/2 * (a + 5); 
     write("O valor de b2 e: "); 
     write (b_2); 
  }
